[package]
name = "whytho-tables"
version = "0.0.1"
edition = "2024"
rust-version = "1.85"
license.workspace = true
description = "Constant tables (CDFs, transform kernels, scan/quant LUTs) from the AV2 spec"
